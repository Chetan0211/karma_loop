#!/bin/bash -e

if [ -f /rails/tmp/pids/server.pid ]; then
  rm -f /rails/tmp/pids/server.pid
fi

# if ! bundle check > /dev/null 2>&1; then
#   echo "==> Missing gems detected! Automatically installing dependencies..."
#   bundle install
# else
#   echo "==> All gems are up to date!"
# fi

./bin/rails db:prepare

bundle exec rails searchkick:create_indexes

exec "${@}"
